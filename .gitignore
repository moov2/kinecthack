# get NPM dependencies by running `npm install`.
/node_modules

# cache created by Sass to speed up compilation
# http://sass-lang.com/documentation/file.SASS_REFERENCE.html#caching
/.sass-cache

# CSS file can be created by running gulp using `gulp` command.
*.css
